{
  "buildCommand": "cd front-end/my-app && npm install && npm run build",
  "outputDirectory": "front-end/my-app/build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/backend/app.py"
    }
  ]
}